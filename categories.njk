---
layout: base.njk
title: カテゴリ一覧
---

<h1>カテゴリ一覧</h1>
{% for category, posts in collections.categories %}
<h2>{{ category }}</h2>
<ul>
  {% for post in posts %}
  <li><a href="{{ post.url | url }}">{{ post.data.title }}</a></li>
  {% endfor %}
</ul>
{% endfor %}